{% set name = "crepes" %}
{% set version = "0.9.1" %}
{% set build = 1 %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/crepes-{{ version }}.tar.gz
  sha256: 14255bfeef3d3cb76fefa78f9cef1d05ecf106dfae6f2bf76096eb7f5524c099

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: {{ build }}

requirements:
  host:
    - python {{ python_min }}
    - setuptools >=61.0
    - wheel
    - pip
  run:
    - python >={{ python_min }}
    - numpy
    - pandas
    - scipy
    - scikit-learn

test:
  imports:
    - crepes
  commands:
    - pip check
  requires:
    - python {{ python_min }}
    - pip

about:
  home: https://github.com/henrikbostrom/crepes
  dev_url: https://github.com/henrikbostrom/crepes
  doc_url: https://crepes.readthedocs.io/en/latest/
  summary: Conformal classifiers, regressors, and predictive systems (crepes)
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - henrikbostrom
    
